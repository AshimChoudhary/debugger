{
  "name": "debug-assistant",
  "version": "1.0.0",
  "description": "AI-powered debugging assistant with Gemini integration",
  "scripts": {
    "build": "npm install --prefix backend && npm install --prefix frontend && npm run build --prefix frontend",
    "start": ""
  },
  "dependencies": {
    "openai": "^5.10.1",
    "concurrently": "^8.2.2"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
